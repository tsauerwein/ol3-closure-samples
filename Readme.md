# Sample projects for Google Closure Compiler and OpenLayers 3

Install the dependencies:

    npm install

Then check the sample projects in the sub-folders. For example start
with the [simple](./simple) project.
